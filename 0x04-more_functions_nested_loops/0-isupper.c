#include "main.h"

/**
 * _isupper - checks if a letter is upper
 * @c: value to be checked
 * Return: 1 for upper and 0 for alse
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
