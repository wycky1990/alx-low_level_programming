#include "main.h"

/**
 * _isupper - checks if a letter is upper
 * @x: value to be checked
 * Return: 1 for upper and 0 for alse
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
