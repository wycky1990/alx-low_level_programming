#include "main.h"

/**
 * _isdigit - fuction that checks for a digits 0 to 9
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 for anything else
 */

int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
