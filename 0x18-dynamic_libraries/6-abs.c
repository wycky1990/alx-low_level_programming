#include "main.h"
/**
 * _abs - function that checks absolute value of an integer
 * @n:int to be checked
 * Return: the absolute value of int
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
